# Configuration only; never put secrets here. Pin reviewed immutable image digests.
ASTER_IMAGE=aster-web:local
PROCESSOR_IMAGE=aster-processor:local
NODE_IMAGE=node:24-trixie-slim
NODE_RUNTIME_IMAGE=gcr.io/distroless/nodejs24-debian13:nonroot@sha256:774b7d020b24214835769e24c3544835526cd0288f0b094eae48e8b2c2429a79
POSTGRES_IMAGE=postgres:17-bookworm
CADDY_IMAGE=caddy:2-alpine
OLLAMA_IMAGE=ollama/ollama:REPLACE_WITH_REVIEWED_VERSION_OR_DIGEST
ASTER_DOMAIN=aster.example.com
BETTER_AUTH_URL=https://aster.example.com
ACME_EMAIL=ops@example.com
ASTER_LOCAL_PORT=3000
ASTER_SECRETS_DIR=./secrets
# Paths must exist. Import is manual; no model is fetched by this stack.
OLLAMA_MODELS_DIR=./local-models
OLLAMA_IMPORT_DIR=./model-import
OLLAMA_MODEL=aster-approved:gguf
OLLAMA_MEMORY=8g
OCR_ENABLED=true
OLLAMA_TIMEOUT_SECONDS=120
# Planner actions (1-64); separate shared structured-call limit (1-96).
MAX_AGENT_STEPS=32
MAX_MODEL_CALLS=64
MAX_PAGE_EXTRACTIONS=2
VISUAL_PAGES_ENABLED=true
MAX_VISUAL_PAGES=6
MAX_VISUAL_BYTES=8388608
MAX_NESTED_EML_DEPTH=3
MAX_EMAIL_PARTS=32
MAX_EMAIL_ATTACHMENTS=8
DOCUMENT_DECODE_TIMEOUT_SECONDS=75
POSTGRES_MEMORY=2g
WEB_MEMORY=1g
WORKER_MEMORY=1g
PROCESSOR_MEMORY=2g
# Existing installations: add an encryption_keyring secret containing {} before upgrading.
ENCRYPTION_ACTIVE_KEY_ID=legacy
# Also start --profile delivery and populate smtp_settings to enable recovery delivery.
EMAIL_DELIVERY_ENABLED=false
# Optional synthetic-only compose.demo.yaml override; directory must already exist.
# ASTER_DEMO_INTAKE_DIR=/absolute/private/aster-demo-intake
