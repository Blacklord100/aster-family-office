# Runtime image inputs only. Keep environments, caches, evaluations, and
# operator documents/secrets out of the build context.
**
!requirements.lock.txt
!runtime/
!runtime/*.py
!runtime/*.sha256
!service/
!service/*.py
!corpus/
!corpus/train.json
